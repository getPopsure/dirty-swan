import Signaturepad from ".";

<Meta title="Components/Signaturepad" component={Signaturepad} />

# Signature pad

Signature pad are user interface elements which allow user sign any legal document.

## Examples

### Default example

<Preview>
  <Signaturepad onChange={() => {}} />
</Preview>

```typescript
<Signaturepad onChange={(base64Signature: string) => {}} />
```
